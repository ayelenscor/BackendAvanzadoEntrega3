// DEPRECATED: este archivo fue sustituido por `cartRepository.js`.
// Conservado temporalmente para evitar rupturas de import.
// Usar `import { CartRepository } from './cartRepository.js'` en su lugar.

export const DEPRECATED = true;