// DEPRECATED: este archivo fue sustituido por `productRepository.js`.
// Conservado temporalmente para evitar rupturas de import.
// Usar `import { ProductRepository } from './productRepository.js'` en su lugar.

export const DEPRECATED = true;